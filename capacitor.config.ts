import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chatfull.app',
  appName: 'chatfull',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
